@model SaphiraTerror.Models.TipoUsuario

@{
    ViewData["Title"] = "Excluir Classificação";
}

<h2>Deseja realmente excluir esta classificação?</h2>

<dl class="row">
    <dt class="col-sm-2">Descrição</dt>
    <dd class="col-sm-10">@Model.DescricaoTipoUsuario</dd>
</dl>

<form asp-action="Delete">
    <input type="hidden" asp-for="IdTipoUsuario" />
    <button type="submit" class="btn btn-danger">Excluir</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>